# CLAUDE.md

Repository instructions live in **[AGENTS.md](AGENTS.md)** — one file, so
Claude Code and Codex work from the same rules. Read it before changing
anything here.

The one rule worth repeating: the skill exists once, at `skills/ui-qa/`.
Everything under `.claude/` and `.codex/` is generated by
`scripts/sync-platform-dirs.sh`. Never hand-edit a generated stub.
