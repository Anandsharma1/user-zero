# GENERATED by scripts/sync-platform-dirs.sh from skills/ui-qa — do not edit.
interface:
  display_name: "ui-qa — human-style UI QA explorer"
  short_description: "Drive the real UI as a senior UX evaluator; produce evidence-backed findings."
  default_prompt: |
    Read skills/ui-qa/SKILL.md in full, then follow it. The evaluator persona is
    skills/ui-qa/agents/user-zero.md and its expertise is
    skills/ui-qa/references/ux-evaluation-taxonomy.md plus any lenses the charter names.
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "playwright"
